# 42-Webserv
A non-blocking HTTP/1.x server in C++98, driven by a single poll() loop, with config-file-driven multi-port serving, GET/POST/DELETE, static files, uploads, directory listing, error pages, CGI, and cookies/sessions as bonus.
